[project]
name = "new-sql"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dash>=4.0.0",
    "pandas>=3.0.1",
    "plotly>=6.6.0",
]
